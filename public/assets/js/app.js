const form = document.getElementById('extractForm');
const regex = '';
let url = document.getElementById('subject').value;


form.addEventListener('submit', (event) => {
  //event.preventDefault();
  console.log(url);
});